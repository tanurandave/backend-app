spring.application.name=${SPRING_APPLICATION_NAME:backend-app}
server.port=${PORT:${SERVER_PORT:8080}}

# ==============================
# MYSQL DATABASE CONFIGURATION
# ==============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://caboose.proxy.rlwy.net:54878/railway?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:SISYaULgBZYgJSFaXuLfZpPahQGdrSJb}

# ==============================
# JPA CONFIGURATION
# ==============================
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==============================
# HIKARI CONNECTION POOL
# ==============================
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=30000

spring.main.allow-circular-references=true

# ==============================
# JWT CONFIGURATION
# ==============================
jwt.secret=${JWT_SECRET:traininginstitutesecretkeywhichisverylongandsecure123456789}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ==============================
# FILE UPLOAD
# ==============================
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# ==============================
# MAIL CONFIGURATION
# ==============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:randavetanuja@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Anuj@_2004}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test-connection=false

# ==============================
# OTP
# ==============================
otp.expiry.minutes=${OTP_EXPIRY_MINUTES:10}